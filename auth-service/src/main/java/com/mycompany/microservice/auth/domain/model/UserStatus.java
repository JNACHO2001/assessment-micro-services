package com.mycompany.microservice.auth.domain.model;

/**
 * Domain enum representing user status.
 * Pure domain - no framework dependencies.
 */
public enum UserStatus {
    ACTIVO,
    INACTIVO
}
